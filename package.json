{
  "scripts": {
    "start": "cd client && yarn start",
    "server": "cd server && yarn start",
    "full": "concurrently \"yarn start\" \"yarn run server\""
  },
  "dependencies": {
    "concurrently": "^5.3.0"
  }
}
